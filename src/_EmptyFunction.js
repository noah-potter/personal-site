import React from 'react'
import { makeStyles } from '@material-ui/styles'

const useStyles = makeStyles({})

export default function EmptyFunction(props) {
  const classes = useStyles()

  return (
    //
    <div />
  )
}
